# 限制请求体大小
client_max_body_size 100M;
client_body_buffer_size 128k;

# 超时设置（keepalive_timeout 已在主配置文件中定义，此处移除避免重复）
client_body_timeout 60s;
client_header_timeout 60s;
send_timeout 60s;

# 隐藏Nginx版本号
server_tokens off;

# 安全头
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "no-referrer-when-downgrade" always;

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 字符集
    charset utf-8;

    # 日志配置
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    # Gzip压缩优化
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1000;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/xhtml+xml
        application/x-font-ttf
        application/vnd.ms-fontobject
        font/opentype
        image/svg+xml
        image/x-icon;
    gzip_disable "msie6";

    # Brotli压缩（如果nginx支持）
    # brotli on;
    # brotli_comp_level 6;
    # brotli_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # 健康检查端点（前端自身健康检查）
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # API代理
    location /api {
        # 使用 /api 前缀，确保路径正确传递
        # 注意：proxy_pass 后面不加斜杠，这样 /api/xxx 会被代理到 http://backend:8000/api/xxx
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        
        # 请求头
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # 重定向处理：将后端返回的重定向URL中的backend:8000替换为当前host
        # 这样客户端收到的重定向URL是正确的
        # 注意：需要处理多种可能的重定向格式
        proxy_redirect http://backend:8000/ $scheme://$host/;
        proxy_redirect http://backend:8000 $scheme://$host;
        proxy_redirect http://backend:8000/api/ $scheme://$host/api/;
        proxy_redirect http://backend:8000/api $scheme://$host/api;
        # 处理相对路径重定向（如果后端返回相对路径）
        proxy_redirect /api/ /api/;
        proxy_redirect /api /api/;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # 缓冲设置
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
        
        # 关闭缓冲以支持流式响应
        proxy_buffering off;
    }

    # 静态资源缓存优化
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
        
        # 启用gzip
        gzip_static on;
    }

    # HTML文件不缓存
    location ~* \.(html|htm)$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        add_header Pragma "no-cache";
    }

    # SPA路由支持
    location / {
        try_files $uri $uri/ /index.html;
        
        # HTML不缓存
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 禁止访问备份文件
    location ~ ~$ {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 错误页面
    error_page 404 /index.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}


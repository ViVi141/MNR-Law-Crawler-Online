# 本地开发环境变量配置示例
# 复制此文件为 .env.local 以覆盖默认配置

# API基础URL
# 开发环境：使用 http://localhost:8000（Vite代理会自动处理）
# 本地预览构建后的代码：使用 http://localhost:8000
# Docker生产环境：留空（使用相对路径，通过Nginx代理）
VITE_API_BASE_URL=http://localhost:8000

